services:
  - type: web
    name: openai-proxy
    runtime: node
    plan: free
    buildCommand: pnpm install && pnpm run build
    startCommand: pnpm run start
    envVars:
      - key: OPENAI_API_KEY
        scope: run
      - key: NODE_ENV
        value: production
